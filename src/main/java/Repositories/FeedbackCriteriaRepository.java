package Repositories;

import Entities.FeedbackCriteria;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FeedbackCriteriaRepository extends JpaRepository<FeedbackCriteria, Long> {
}
